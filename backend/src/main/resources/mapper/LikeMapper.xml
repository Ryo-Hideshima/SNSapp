<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snsapp.backend.mapper.LikeMapper">

    <select id="existsByPostIdAndUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
        )
    </select>

    <!--
        競合対象の列を明示しない ON CONFLICT DO NOTHING にしているのは、PostgreSQLだけでなく
        H2(テスト用DB、PostgreSQL互換モード)でも同一SQLをそのまま実行できるようにするため
        (H2は競合列を明示した ON CONFLICT (col, ...) DO NOTHING を受け付けない)。
        likes テーブルには(post_id, user_id)のUNIQUE制約しか無いため、挙動は変わらない。
    -->
    <insert id="insertIfAbsent">
        INSERT INTO likes (post_id, user_id, created_at)
        VALUES (#{postId}, #{userId}, now())
        ON CONFLICT DO NOTHING
    </insert>

    <delete id="deleteByPostIdAndUserId">
        DELETE FROM likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="countByPostId" resultType="long">
        SELECT COUNT(*) FROM likes WHERE post_id = #{postId}
    </select>

</mapper>
